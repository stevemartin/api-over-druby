== README

* rake db:migrate
* rake db:seed
* rake drb:api_server

Then open up another console ( or tmux window ) and navigate to the root of this app, then in the console, do...

    rails c
    require 'drb_client'
    @api_client.author_class
    @api_client.author( 1 )
    @api_client.author_json( 1 )
    @api_client.authors_json
    @api_client.authors_hash

    etc ...

TA DA!! JSON API OVER DRB!


